import type { LayoutServerLoad } from './$types';

export const load = (async () => {
  // const router = await server(event);
  // const procedureData = await promise(() => router.guest.authenticate());
  // if (!procedureData.success) {
  //   throw redirectOnUnAuthorized(procedureData.error, '/admin/auth/dashboard');
  // }
}) satisfies LayoutServerLoad;
